// Region is always us-east-1 because that's the only region AWS Lambda@Edge
// functions can be deployed in.
export const AWS_REGION = "us-east-1";

export const AWS_ACCOUNT_ID = 1337;

export const AWS_LAMBDA_FUNCTION_VERSION = 1;

export const AWS_LAMBDA_FUNCTION_MEMORY_SIZE = 128;
